/* usaremos para calcular circunferência
 * é necessário o objeto Math para calcular
 * exponenciação
 */

const raio = 5.6;
const area = Math.PI * Math.pow(raio, 2);

console.log(area);
console.log(area.toFixed(3));
console.log(typeof Math); // objeto
