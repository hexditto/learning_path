// Aritméticos, são binários =  operam em cima de dois operandos 

const [a, b, c, d] = [3, 5, 1, 15];

const soma = a + b + c + d; // a operação ocorre em duplas
const subtracao = d - b;
const multiplicacao = a * b;
const divisao = d / a;
const modulo = a % 2;

console.log(soma, subtracao, multiplicacao, -divisao, modulo);
